You are building a production-ready web app for a local tailoring business.

Project Name: RK Studio
Location: Narnaul, Haryana (123001)

Tech Stack:

* Next.js (App Router)
* React functional components
* Material UI (MUI)

Design System:

* Primary Color: #1E3A8A (Deep Blue)
* Background: #F8F5F0 (Beige)
* Accent: #D97706 (Mustard)
* Clean, minimal, high readability UI
* Use consistent spacing, padding, and typography

UI/UX Rules:

* Keep UI simple and easy for non-technical users
* Use large buttons and clear labels
* Avoid complex animations
* Mobile-first responsive design
* Reusable components only (no duplicate UI code)

Architecture Rules:

* Use proper folder structure:

  * components/
  * app/
  * features/
  * services/
  * utils/
* Separate UI and logic
* Use clean and readable code

Performance Rules:

* Use lazy loading with Next.js dynamic imports
* Use code splitting for pages and heavy components
* Use next/image for optimized images
* Avoid unnecessary re-renders

Components Standard:

* Navbar, Footer must be reusable
* Create reusable:

  * Button
  * Card
  * ProductCard
  * CategoryCard
  * Stepper
* Use props for flexibility

Pages:

* Homepage
* Tailoring
* Fabric
* Dupatta

Business Logic:

* Tailoring must use multi-step form
* Fabric must use product grid + filters
* Dupatta must be simple grid + add to cart

Localization:

* Use simple Hinglish text
* Show "Serving Narnaul (123001)"
* Add WhatsApp integration everywhere

WhatsApp:

* Floating button on all pages
* Message: "Hello RK Studio, mujhe silai / kapda ke bare me jankari chahiye."

Code Quality:

* Use clean naming conventions
* Add comments where needed
* Avoid unnecessary complexity

Goal:
Build a fast, simple, scalable web app that feels like a local tailoring shop.
